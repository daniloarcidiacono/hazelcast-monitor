version: 2
jobs:
  build_prepare:
    docker:
      - image: daniloarcidiacono/ci-java-node:0.4.0

    working_directory: ~/hazelcast-monitor

    steps:
      - checkout

      - persist_to_workspace:
          root: '~'
          paths:
            - hazelcast-monitor

  build_fe:
    docker:
      - image: daniloarcidiacono/ci-java-node:0.4.0

    working_directory: ~/hazelcast-monitor

    steps:
      - attach_workspace:
          at: '~'

      - restore_cache:
          key: npm-dependency-cache-{{ checksum "hazelcast-monitor-webapp/package.json" }}

      - run:
          name: Install npm packages
          command: cd hazelcast-monitor-webapp && npm install

      - save_cache:
          key: npm-dependency-cache-{{ checksum "hazelcast-monitor-webapp/package.json" }}
          paths:
            - hazelcast-monitor-webapp/node_modules

      - run:
          name: Build prod
          command: cd hazelcast-monitor-webapp && npm run build-prod

      - run:
          name: Zipping
          command: cd hazelcast-monitor-webapp/dist && zip -r hazelcast-monitor-$CIRCLE_TAG.zip hazelcast-monitor

      - store_artifacts:
          path: hazelcast-monitor-webapp/dist/hazelcast-monitor-$CIRCLE_TAG.zip

  build_be:
    docker:
      - image: daniloarcidiacono/ci-java-node:0.4.0

    working_directory: ~/hazelcast-monitor

    steps:
      - attach_workspace:
          at: '~'

      - restore_cache:
          key: hazelcast-monitor-{{ checksum "pom.xml" }}

      - run:
          command: mvn verify dependency:go-offline

      - save_cache:
          paths:
            - ~/.m2
          key: hazelcast-monitor-{{ checksum "pom.xml" }}

      - run:
          command: mvn clean package -Pdocs

      - deploy:
          name: Create GitHub release
          command: |
            hub release create -d \
                               -a hazelcast-monitor-agent/target/hazelcast-monitor-agent-$CIRCLE_TAG-javadoc.jar \
                               -a hazelcast-monitor-starter-java-websockets/target/hazelcast-monitor-starter-java-websockets-$CIRCLE_TAG-javadoc.jar \
                               -a hazelcast-monitor-starter-spring/target/hazelcast-monitor-starter-spring-$CIRCLE_TAG-javadoc.jar \
                               -F CHANGELOG_LATEST.md $CIRCLE_TAG

      - store_artifacts:
          path: hazelcast-monitor-agent/target/hazelcast-monitor-agent-$CIRCLE_TAG.jar

      - store_artifacts:
          path: hazelcast-monitor-starter-sample-app/target/hazelcast-monitor-starter-sample-app-$CIRCLE_TAG.jar

      - store_artifacts:
          path: hazelcast-monitor-starter-java-websockets/target/hazelcast-monitor-starter-java-websockets-$CIRCLE_TAG.jar

      - store_artifacts:
          path: hazelcast-monitor-starter-spring/target/hazelcast-monitor-starter-spring-$CIRCLE_TAG.jar

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build_prepare:
          filters:
            tags:
              only: /^pre.*/
            branches:
              ignore: /.*/

      - build_fe:
          requires:
            - build_prepare
          filters:
            tags:
              only: /^pre.*/
            branches:
              ignore: /.*/

      - build_be:
          requires:
            - build_prepare
          filters:
            tags:
              only: /^pre.*/
            branches:
              ignore: /.*/
